struct testStruct {
 item1: i32,
 item2: i32
};

contract hello {
    test(arg1: i32): i32;
};

impl testStruct: hello {
    test(arg1: i32): i32 {
        return 3+arg1;
    };
};

lol(arg1: testStruct): testStruct {
    return arg1;
};

main() {
  uwu = testStruct {
    item1 = 3,
    item2 = 4
  };

for(i = 0; i < 10; inc(i)) {
println( i + uwu.item1);
};
  };
};